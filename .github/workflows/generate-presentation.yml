name: Generate executive presentation

on:
  push:
    branches: [main]
    paths:
      - 'scripts/generate_presentation_ci.js'
      - '.github/workflows/generate-presentation.yml'

permissions:
  contents: write

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Install presentation dependency
        run: npm install --no-save pptxgenjs

      - name: Generate PowerPoint
        run: node scripts/generate_presentation_ci.js

      - name: Commit generated presentation
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/presentacion-ejecutiva.pptx
          if git diff --cached --quiet; then
            echo "Presentation already up to date"
          else
            git commit -m "docs: generar presentación ejecutiva"
            git push
          fi
